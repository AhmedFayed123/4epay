import 'package:flutter/material.dart';

class NearestTradersList extends StatelessWidget {
  const NearestTradersList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
